<!--
  CLAUDE.md 範本 — 複製到專案根目錄改名為 CLAUDE.md,照註解填,填完把註解刪掉。

  ⚠️ 三個原則,填之前先讀:
  1. 這是**唯一每次對話自動載入**的檔案 → 只放「必須當場生效」的東西。
     深度知識寫 docs/dev/*.md,這裡只留摘要 +「動到 X 之前先讀 Y」的觸發語。
  2. **每條規則附上為什麼**(實測數據 / 上游行為 / 災情)。少了理由,
     下一次重構就會被順手改掉——包括被 AI 順手改掉。
  3. **第 1 天不要寫滿**。目標 100 行以內,之後每踩一個坑長一條。
     一開始就照抄別的專案 = 一份沒有人遵守的教條。
-->

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 專案概述

<!--
  三到五句話講清楚:這個東西是什麼、給誰用、最重要的一條產品原則是什麼。
  ⚠️ 特別要寫的是「反直覺的定位」——例如「產物的主要消費者是 AI 不是人」,
  那一句會反向決定保真度、格式與結構,是最容易被忘記、忘了就整段白做的東西。

  範例(來源專案):
    地端會議錄音轉逐字稿工具,目標使用者是非技術同仁。
    隱私是硬性規格:只綁 127.0.0.1、遙測全關,連網行為只有「首次下載模型」。
-->

## 常用指令

<!--
  只列「每次都會用到」的那幾條。安裝、啟動、測試、單一測試、覆蓋率。
  ⚠️ 連同「怎麼跑單一測試」一起寫——不寫的話每次都會跑整套,浪費的是你的時間。

  範例:
    - 建環境:`uv sync`
    - 測試:`uv run pytest`(預設排除 slow 標記)
    - 單一測試:`uv run pytest tests/test_x.py::test_名稱 -q`
    - 測試的反向稽核:`uv run python scripts/mutate.py`(約 1 分鐘)
-->

## 架構

<!--
  ⚠️ 不要寫成「檔案清單」——那個 AI 自己看得到,寫了只是浪費預算。
  要寫的是**看程式碼看不出來的東西**:

  - 資料流的主線(A → B → C,每一段做什麼)
  - 為什麼是這個設計(尤其是「看起來多餘、其實不能拿掉」的部分)
  - ⚠️ 已經結案的決定 +「勿再重開」(附裁決日期與實測理由)
  - 「動到 X 之前先讀 docs/dev/y.md」的觸發語

  範例:
    - ⚠️ 兩顆模型各司其職,不得「順手統一」:A 只影響切分邊界、抽出的結果全丟棄,
      要進資料庫的一律是 B——換掉後者等於讓整個資料庫失效。
    - ⚠️ 以下已結案、勿再重開:beam_size 維持 5(2026-xx-xx 實測裁決,理由見 …)
-->

## 協作方式

<!--
  「怎麼跟我一起工作」。完整版放 docs/dev/collaboration.md(見本 Skill 的 assets),
  但**必須當場生效**的幾條要抄一句在這裡——只寫在 docs 裡等於沒寫,
  那一份要有人想到去讀才會載入。

  典型的幾條:
  - 回覆用什麼語言
  - 規格討論要主動補盲點(分「強烈建議 / 順手幾乎免費 / 先不做」三級)
  - 「產物給誰用」要在最前面問
  - UI/版面改動先出設計稿讓我選案,通過才動程式碼
  - 改了使用者看得到的行為,就主動更新說明文件,不必等我提醒
  - 「請記住這件事」= 寫進 repo(這個檔或 docs/),不是寫進記憶功能
-->

## 硬性慣例

<!--
  違反就是錯的那些。每一條都要有「為什麼」,而且**最好有測試守著**
  (見本 Skill 的紀律二;守不到的才留在這裡當規則)。

  這一節是**隨踩隨長**的主戰場——每次踩到坑就在這裡加一條。
  升級階梯:規則 → 機械步驟 → 測試 → hook。同一條失守一次就升一級。

  範例(格式參考,內容一定要換成你自己的):
    - **使用者可見訊息一律 <語言>**:自家錯誤用 UserFacingError(可原樣顯示);
      第三方例外是 cryptic 英文,必須包裝過再顯示。
    - **改文案 = 拿舊字串 grep -rn 全 repo**,改完再搜一次確認零殘留。
      不要憑記得去改那幾個檔——連著改五個標籤時,第五個一定會漏。
      搜的範圍要含 .md(README 與 docs 都可能引著同一句話)。
    - **測試不得探測真實硬體、不得動到使用者的真實資料**(見 tests/conftest.py)。
-->
