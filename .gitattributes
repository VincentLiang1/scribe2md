# 公開 repo 專用的 .gitattributes(由「打包.bat」複製成 dist\meeting-scribe\.gitattributes)。
#
# 只留這一條。⚠️ **主 repo 那份的 `export-ignore` 幾行不可以抄過來**:它們會讓
# 公開 repo 的「Download ZIP」少掉 `docs\` 與 `scripts\`,而 `scripts\install_skill.py`
# 正是「安裝Skill.bat」要呼叫的對象(少了它那支 bat 直接失敗),
# `docs\claude-privacy-setting.jpg` 也是「使用說明」分頁的插圖。
# 主 repo 留著那幾行是因為那邊不走 Download ZIP,公開 repo 剛好相反。
*.bat text eol=crlf
